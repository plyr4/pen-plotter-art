Things to do, questions to ask, art to create, etc.

- What should we set for plotter config? (Paper size, width, height, center, velocity)
- What does `device` do?
- How do we supply a custom `device` ?
> [vpype docs](https://vpype.readthedocs.io/en/latest/reference.html#write) say: When writing to HPGL, a device name must be provided with the --device option. The corresponding device must be configured in the built-in or a user-provided configuration file (see the documentation for more details). The following devices are currently available:
> ```hp7475a, designmate, hp7440a, artisan, dmp_161, hp7550, dxy, sketchmate```

- Does our art generator (lines) respect the page size?
